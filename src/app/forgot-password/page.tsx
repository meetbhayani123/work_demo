import EmailForm from "@/components/forgot-password/EmailForm";

export default function ForgotPasswordPage() {
    return (
        <main className="flex min-h-screen flex-col items-center justify-center p-4 page-gradient">
            <EmailForm />
        </main>
    );
}
